<?php
$values = $block->getValues();
$settings = (isset($values['settings']['story_block'])) ? $values['settings']['story_block'] : [] ;
$placement = (isset($settings['position'])) ? $settings['position'] : '';
?>

<div class="story_block">
    <div class="placement">
        <div class="field">
            <label class="option-title">
                <?= /* @noEscape */ __("Player Placements") ?>
            </label>
            <div class="control">
                <ul class="field-options">
                    <li>
                        <input
                            type="radio"
                            id="story_block-position-bottom_right"
                            name="parameters[data][settings][story_block][position]"
                            value="bottom-right" <?= ($placement == 'bottom_right') ? "checked" : '' ?>
                        />
                        <label for="story_block-position-bottom_right">
                            <?= /* @noEscape */ __('Bottom Right') ?>
                        </label>
                    </li>
                    <li>
                        <input
                            type="radio"
                            id="story_block-position-bottom_left"
                            name="parameters[data][settings][story_block][position]"
                            value="bottom-left" <?= ($placement == 'bottom_left') ? "checked" : '' ?>
                        />
                        <label for="story_block-position-bottom_left">
                            <?= /* @noEscape */ __('Bottom Left') ?>
                        </label>
                    </li>
                    <li>
                        <input
                            type="radio"
                            id="story_block-position-top_right"
                            name="parameters[data][settings][story_block][position]"
                            value="top-right" <?= ($placement == 'top_right') ? "checked" : '' ?>
                        />
                        <label for="story_block-position-top_right">
                            <?= /* @noEscape */ __('Top Right') ?>
                        </label>
                    </li>
                    <li>
                        <input
                            type="radio"
                            id="story_block-position-top_left"
                            name="parameters[data][settings][story_block][position]"
                            value="top-left" <?= ($placement == 'top_left') ? "checked" : '' ?>
                        />
                        <label for="story_block-position-top_left">
                            <?= /* @noEscape */ __('Top Left') ?>
                        </label>
                    </li>
                </ul>
            </div>
        </div>
    </div>
</div>